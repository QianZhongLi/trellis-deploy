# TRELLIS Runtime (12GB GPU optimized) — core deps from trellis-venv
# Install: pip install -r requirements.txt
# Then clone/copy TRELLIS source tree (included in this repo) and run setup.sh

# --- Core ML ---
torch==2.4.0+cu121
torchvision==0.19.0+cu121
--extra-index-url https://download.pytorch.org/whl/cu121
timm

# --- TRELLIS 3D extensions (build from source via TRELLIS/setup.sh) ---
# cumm, spconv, torchsparse, diff_gaussian_rasterization, diffoctreerast, flexicubes
cumm-cu121
spconv-cu121
torchsparse

# --- App / API ---
fastapi==0.111.0
uvicorn==0.52.3
python-multipart==0.0.32
pydantic==2.13.4

# --- Imaging / geometry ---
pillow==10.4.0
numpy==2.2.6
trimesh==5.0.0
scipy==1.15.3
progressbar2
einops==0.8.2
huggingface_hub

# --- Optional web ---
gradio==4.44.1
